# 🌌 TranslatorForLincos

Нейросетевой переводчик с русского языка на искусственный язык **Lincos** — язык для межзвёздной коммуникации, включающий математические и логические конструкции.

Этот проект реализует нейросетевой переводчик с русского языка на **Lincos** — искусственный язык, разработанный для межзвёздной коммуникации. Lincos основан на логике, математике и универсальных понятиях, включая символы вроде `∈`, `∀`, `∃`, `⇒`, `∧`, `∨`, `¬`, а также арифметические операции (`+`, `-`, `×`, `÷`, `=`).

## 💡 Проект включает:

- Обучение Seq2Seq-модели с LSTM   
- Консольные утилиты для анализа и тестирования  
- Графический интерфейс пользователя (GUI)

## 📦 Требования

1. Перед запуском убедитесь, что у вас установлены:
   - **Python 3.8+**
   - **pip** (менеджер пакетов Python)

2. Рекомендуется использовать виртуальное окружение:

   ```bash
   python -m venv venv
   ```
   Lincos/macOS
   ```bash
   source venv/bin/activate
   ```
   Windows
   ```bash
   source venv/Scripts/activate
   ```
   
4. Установите зависимости:

   ```bash
   pip install -r requirements.txt

5. Запуск пользовательского интерфейса:

   ```bash
   python visual_for_user.py
